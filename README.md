# Gemini CLI API Gateway

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![Node](https://img.shields.io/badge/node-%3E%3D20-green.svg)
![Docker](https://img.shields.io/badge/docker-ready-blue.svg)
![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)

**Gemini CLI API Gateway** — это высокопроизводительный микросервис на Node.js, который предоставляет REST API интерфейс для консольной утилиты `@google/gemini-cli`.

Проект разработан для запуска в контейнеризированных средах (Docker) и оптимизирован для работы на ARM-архитектуре (Raspberry Pi), обеспечивая удобное взаимодействие с нейросетями Google Gemini без необходимости постоянной авторизации через браузер.

## 🚀 Возможности

- **RESTful API**: Стандартные JSON-запросы для интеграции с любыми фронтендами и сервисами.
- **SSE Streaming**: Поддержка Server-Sent Events для потокового получения ответа (эффект печатания текста в реальном времени).
- **Clean Architecture**: Код разделен на слои (Config, Controllers, Services) по принципам SOLID.
- **Docker Ready**: Полная изоляция окружения и зависимостей.
- **Zero-Config Auth**: Использование существующей сессии авторизации через Docker Volumes.
- **ENV Configuration**: Все настройки вынесены в переменные окружения.
- **Watchdog**: Защита от зависших процессов CLI (автоматический таймаут).

---

## 📋 Предварительные требования

Перед запуском убедитесь, что у вас установлены:

1.  **Docker** и **Docker Compose**.
2.  **Авторизованная сессия Gemini CLI** на хост-машине (папка `~/.gemini` с ключами).

---

## 🛠 Установка и Запуск

### 1. Подготовка проекта

Создайте директорию проекта и поместите туда файлы исходного кода.

### 2. Настройка авторизации

Скопируйте вашу локальную папку с ключами авторизации в корень проекта. Это необходимо для того, чтобы Docker-контейнер мог использовать ваши учетные данные.

```bash
cp -r ~/.gemini .
```

> **Важно:** Убедитесь, что папка `.gemini` добавлена в `.gitignore`, чтобы не скомпрометировать ваши ключи доступа.

### 3. Конфигурация (.env)

Используйте пример конфигурации для быстрого старта:

```bash
cp .env.example .env
```

Затем откройте файл `.env` и настройте параметры под свои нужды:

```dotenv
# Порт сервера
PORT=3000

# Окружение (production/development)
NODE_ENV=production

# Модель по умолчанию (если не указана в запросе)
GEMINI_DEFAULT_MODEL=gemini-2.5-flash-lite

# Лимит времени на генерацию (в мс)
GEMINI_REQUEST_TIMEOUT_MS=60000

# Команда запуска (обычно 'gemini')
GEMINI_CLI_COMMAND=gemini

# Максимальное количество одновременных запросов
GEMINI_MAX_CONCURRENT_REQUESTS=2
```

### 4. Запуск в Docker

Соберите и запустите контейнер в фоновом режиме:

```bash
docker-compose up -d --build
```

---

## 🔌 API Документация

> **Интерактивная документация:** Откройте `/api/docs` в браузере, чтобы увидеть **Swagger UI** — красивый интерфейс для тестирования запросов и чтения спецификации.

> **Совет:** В корне проекта есть файл `requests.http`. Если вы используете WebStorm, VS Code (с плагином REST Client) или IntelliJ IDEA, вы можете запускать запросы прямо из этого файла нажатием одной кнопки.

### 1. Проверка здоровья (Health Check)

Используется для мониторинга состояния сервиса и доступности CLI.

- **URL:** `/api/health`
- **Method:** `GET`
- **Response:**

```json
{
  "status": "ok",
  "uptime": 125.5,
  "timestamp": "2026-01-09T16:00:00.000Z"
}
```

### 2. Список моделей

Возвращает список доступных моделей и модель, используемую по умолчанию.

- **URL:** `/api/models`
- **Method:** `GET`
- **Response:**

```json
{
  "default_model": "gemini-2.5-flash-lite",
  "available_models": [
    {
      "id": "gemini-2.5-flash-lite",
      "name": "Gemini 2.5 Flash Lite",
      "description": "Fastest..."
    },
    {
      "id": "gemini-2.5-flash",
      "name": "Gemini 2.5 Flash",
      "description": "Balanced..."
    }
  ]
}
```

### 3. Чат (Стандартный)

Ожидает полного завершения генерации и возвращает готовый ответ целиком.

- **URL:** `/api/chat`
- **Method:** `POST`
- **Content-Type:** `application/json`
- **Body Parameters:**
- `prompt` (string, required): Текст запроса.
- `system` (string, optional): Системная инструкция/роль.
- `model` (string, optional): ID модели (например, `gemini-1.5-pro`).

- **Пример запроса (cURL):**

```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Расскажи шутку про Linux",
    "system": "Ты веселый робот",
    "model": "gemini-2.5-flash"
  }'
```

- **Response:**

```json
{
  "status": "success",
  "model": "gemini-2.5-flash",
  "response": "Почему пингвины не используют Windows? Потому что они боятся, что окна замерзнут!"
}
```

### 4. Чат (Потоковый / Streaming)

Возвращает ответ по частям в реальном времени, используя технологию **Server-Sent Events (SSE)**.

- **URL:** `/api/chat/stream`
- **Method:** `POST`
- **Body Parameters:** Те же, что и для обычного чата.
- **Пример запроса (cURL):**
  > Обратите внимание на флаг `-N`, отключающий буферизацию.

```bash
curl -N -X POST http://localhost:3000/api/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Напиши поэму"}'
```

- **Формат потока:**
  Сервер присылает события `message` с JSON-данными. В конце потока присылается событие `done`.

```text
data: {"type": "message", "content": "В", "role": "assistant"}

data: {"type": "message", "content": " лесу", "role": "assistant"}

...

event: done
data: [DONE]
```

---

## 📂 Структура проекта

Проект организован в соответствии с MVC-подобной архитектурой:

```text
gemini-service/
├── config/                 # Конфигурация
│   └── app.config.js       # Чтение .env и дефолтные настройки
├── controllers/            # Контроллеры (Обработка HTTP запросов)
│   └── chat.controller.js  # Логика валидации и ответов
├── services/               # Сервисы (Бизнес-логика)
│   └── gemini.service.js   # Взаимодействие с OS процессами (spawn)
├── routes/                 # Маршрутизация
│   └── api.routes.js       # Определение эндпоинтов
├── .gemini/                # Папка с ключами (монтируется в Docker)
├── app.js                  # Точка входа в приложение
├── Dockerfile              # Инструкция сборки образа
├── docker-compose.yml      # Оркестрация контейнера
├── .env                    # Переменные окружения
└── package.json            # Зависимости
```

## 🛡 Безопасность и Рекомендации

1. **API Keys**: Никогда не публикуйте папку `.gemini` в публичные репозитории.
2. **CORS**: В текущей конфигурации CORS разрешен для всех (`*`). Для продакшена рекомендуется ограничить список доменов в `app.js`.
3. **Ресурсы**: На Raspberry Pi с малым объемом памяти (1GB) рекомендуется ограничить ресурсы контейнера в `docker-compose.yml`.

## 📄 Лицензия

MIT License.
